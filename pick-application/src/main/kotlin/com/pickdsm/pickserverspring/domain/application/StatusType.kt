package com.pickdsm.pickserverspring.domain.application

enum class StatusType {
    DISALLOWED, LEAVE, HOME, FIELD_TRIP, EMPLOYMENT, PICNIC, MOVEMENT, AWAIT, PICNIC_REJECT, ATTENDANCE, NOT_COMEBACK,
}
